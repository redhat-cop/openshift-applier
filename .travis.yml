sudo: required

language: ruby

services:
  - docker

before_install:
- docker build -t redhatcop/openshift-applier -f images/openshift-applier/Dockerfile .

script:
- docker run -e INVENTORY_PATH=/openshift-applier/test/inventory/ -ti redhatcop/openshift-applier
